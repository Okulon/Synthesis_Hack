.env
.env.local
.env.*.local

# Vite dashboard (`frontend/`) — same rules; explicit paths for root `git status`
frontend/.env
frontend/.env.local
frontend/.env.*.local

# Optional per-developer config overrides
config/local/

# Common tool artifacts (tune when you add stacks)
.pnpm-store/
node_modules/
dist/
build/
out/

# Foundry: forge script cache/broadcast may contain private keys (never commit)
contracts/cache/
contracts/broadcast/

*.log

# OS / IDE
.DS_Store
.idea/
.vscode/
